{
LONG abcdefghijklmnopq = -214748_l, *{ primer valor permitido dentro del rango, 17 caracteres  }*
UINT bglhotkgiu = 65535_ui, *{ ultimo valor permitido dentro del rango, 10 caracteres }*
UINT _identificador1 = 10_ui,
DOUBLE f = 10.34D+5,
DOUBLE f2 = .34D+7,

*{Este programa sirve para comprobar el funcionamiento completo del compilador}*

INTERFACE inter1{
   VOID m1 (),
},

CLASS abc IMPLEMENT my_interface {
    UINT x; y,
    VOID b (LONG l),
}

IMPL FOR abc : {
    VOID b (LONG l) {
         IF ( x == y ) 
              PRINT % Son iguales !!! %,
         END_IF,
         PRINT % Termina b%,
         RETURN,
     }
}

VOID f ( ) { 
     VOID g () {
         UINT x,
         FOR x IN RANGE (0_ui; 10_ui; 1_ui) 
            PRINT % iterando chill %, 
         PRINT % sali del for %,
         RETURN,
     }

     IF ( abcdefghijklmnopq < bglhotkgiu ) 
              PRINT % Es menor, dentro de f () %,
         END_IF,
    g(),
    RETURN,
}

*{Aca termina el programa}*
}